# This is a small python script that sorts your files based on your preferences(directories).
To be able to run the script, you'll need to install dotenv library.
After that you need to specify in .env file desired destination points.